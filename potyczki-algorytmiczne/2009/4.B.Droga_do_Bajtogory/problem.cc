#include <iostream>
#include <string>
using namespace std;

int main()
{
	int n, k;
	scanf("%d%d", &n, &k);
	for (int i = 0; i < n; i++) {
		int m, a, b;
		scanf("%d", &m);
		for (int j = 0; j < m; j++)
		{
			scanf("%d%d", &a, &b);
		}
	}
	return 0;
}
